name: sass_migrator
version: 0.0.1-dev
description: A tool for running migrations on Sass files

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  args: "^1.4.0"
  sass: "^1.17.3"
  source_span: "^1.4.0"
  path: "^1.6.0"

dev_dependencies:
  term_glyph: "^1.1.0"
  test: ">=0.12.29 <2.0.0"
  test_descriptor: "^1.1.1"
